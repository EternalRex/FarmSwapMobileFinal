import 'package:flutter/material.dart';

class PlaceOrderTextField extends StatelessWidget {
  const PlaceOrderTextField({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
